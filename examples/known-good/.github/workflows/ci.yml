name: ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      RUN_DB_TESTS: "1"   # G8.2 clean: the env var gating the DB test IS set here.
    steps:
      - uses: actions/checkout@v4
      - run: pip install pytest
      - run: pytest
